const {Connection  , } = require("@solana/spl-token")

const connection = new Connection(clusterApiUrl("devnet"))



export const mintoken = async()=>{

console.log("making the token ")
// make  a token 

// make a associate token account 

// then mint to 
}